part of '../bloc/shift_handover_bloc.dart';

//- ACCESS HOME
extension on ShiftHandoverBloc {
  void _accessHome(AccessHome event, Emitter<ShiftHandoverState> emit) {
    Debugger.white('$ShiftHandoverBloc is accessing home');
  }
}
